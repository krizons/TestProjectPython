version: '3.8'
services:

  app:
    container_name: app
   #image: app
    build: .
    command: bash -c "alembic upgrade head  &&  uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
